server.port=8081
spring.application.name=auth-service
spring.jackson.time-zone=Asia/Hong_Kong
spring.jackson.serialization.write-dates-as-timestamps=false
spring.datasource.url=jdbc:mysql://localhost:3306/shareudb?serverTimezone=Asia/Hong_Kong
spring.datasource.username=root
spring.datasource.password=ShareUComp4442
spring.flyway.enabled=false

# Mail settings - DO NOT commit real credentials. Set via environment variables in production.
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:your-email@example.com}
spring.mail.password=${MAIL_PASSWORD:your-email-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
